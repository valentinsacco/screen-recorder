const Header = () => (
    <header className="h-20 w-screen">
        <nav className="h-full w-full px-20 flex flex-row justify-between items-center">
            
        </nav>
    </header>
)

export default Header